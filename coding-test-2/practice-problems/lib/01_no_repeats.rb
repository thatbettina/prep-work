def no_repeats(year_start, year_end)

	'no_repeat?(year)'

	loop do go through all years from year_start to year_end
		split the number into single integers
		compare each integer with each other integer
		stop when you find integers that match
		add combined year to hash when you don't
end
